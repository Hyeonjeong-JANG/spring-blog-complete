{{> /layout/header}}

<div class="container p-5">

    <!-- 요청을 하면 localhost:8080/join POST로 요청됨
    username=사용자입력값&password=사용자값&email=사용자입력값 -->

    <div class="card">
        <div class="card-header"><b>회원가입을 해주세요</b></div>
        <div class="card-body">
            <form action="/join" method="post" onsubmit="return valid()">
                <div class="mb-3">
                    <input id="username" type="text" class="form-control" placeholder="Enter username" name="username"
                           onkeyup="usernameSameCheck()">
                    <div class="d-flex"><div id="invalidMsg" class="text-danger"></div>
                        <button type="button" id="btnUsernameCheck" class="btn btn-warning">아이디 중복 체크</button>
                    </div>
                    <script>

                        // 다시 정리
                        // 1. 초기 세팅
                        let username = $("#username").val();
                        $("#invalidMsg").text("유저네임을 입력해 주세요.");

                        // 2. 아이디 체크


                        // 3. 밑에 메시지


                        // let username = $("#username").val();
                        // let isNotSameUsername = false; // 회원이름이같지않니? 아니! 같아.(default)
                        // // function usernameSameCheck(){
                        // //     if (username==)
                        // // }
                        // function valid() {
                        //     if (isNotSameUsername==false){
                        //         $("#invalidMsg").text("이미 있는 이름, 가입 불가");
                        //     }
                        //     return isNotSameUsername;
                        //     // $("#invalidMsg").text("이미 있는 이름, 가입 불가");
                        // }
                        // $.ajax({
                        //     url: "/api/username-same-check"+username,
                        //     type: "get"
                        // }).done((res) => {
                        //     if (res.body == true) {
                        //         isNotSameUsername = false;
                        //
                        //
                        //     } else {
                        //         isNotSameUsername = true;
                        //         // $("#invalidMsg").text("가입 가능");
                        //     }
                        // }).fail((res) => {
                        //    // location.reload();
                        // });
                        //
                        //

                    </script>
                </div>
                <div class="mb-3">
                    <input type="password" class="form-control" placeholder="Enter password" name="password"
                           value="1234">
                </div>
                <div class="mb-3">
                    <input type="email" class="form-control" placeholder="Enter email" name="email"
                           value="ssar@nate.com">
                </div>
                <button type="submit" class="btn btn-primary form-control">회원가입</button>
            </form>
        </div>
    </div>
</div>

{{> /layout/footer}}